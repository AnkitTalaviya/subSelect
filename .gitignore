node_modules/
dist/
# Copy of dist/ that scripts/store-assets.mjs loads to capture the listing screenshots
dist-store/
*.zip
# Unzipped build output, e.g. from extracting subselect-<version>.zip to load unpacked
subselect-*/
.DS_Store
verify.png

# Generated by `npm run icons`
public/icons/*.png

# Chrome Web Store listing copy and captured assets — kept out of the repository on
# purpose. Regenerate with `node scripts/store-assets.mjs`.
store/

# Local editor / tooling state
.qodo/
.claude/
